<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.douzone.dzfinal.repository.ChatRepository">
	<select id="chatRoomList" resultType="com.douzone.dzfinal.dto.ChatDTO$ChatRoom">
		SELECT * FROM ChatRoom
	</select>
	
	<select id="getChatRoomMessages" resultType="com.douzone.dzfinal.dto.ChatDTO$Message">
		SELECT * FROM Chat WHERE chatroom_id = #{chatroom_id}
	</select>
	
	<insert id="insert">
		INSERT INTO Chat(chatroom_id, message, created_at, `from`) VALUES (#{to}, #{message}, now(), #{from});
	</insert>
	
	
</mapper>